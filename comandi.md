# crezione nuova feature
git checkout main
git fetch origin
git checkout -b new-feature
 -- modifiche + commit + push( commit = "#1 bla bla")
git checkout main
git fetch origin
git merge new-feature
git push origin main

